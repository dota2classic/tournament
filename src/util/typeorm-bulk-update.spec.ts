import { typeormBulkUpdate } from './typeorm-bulk-update';
import { TournamentRegistrationEntity } from '../db/entity/tournament-registration.entity';

describe('typeorm bulk update', () => {
  it('should do stuff', () => {
    // typeormBulkUpdate(TournamentRegistrationEntity, []);
  });
})
